---
source: tests/emit_quality.rs
expression: functions.trim()
---
//! Translated by compylr.

use std::collections::{HashMap, HashSet};

use crate::compat::{
    IndexOrigin, PyAdd, PyContains, PyIterate, PyLen, PyNum, PySetItem, RuntimeError,
    TextUnits, div_exact, py_subscript,
};

pub fn compare_mixed(a: i64, b: f64) -> Result<bool, RuntimeError> {
    Ok(((&(((a) as f64))) < (&(b))))
}

pub fn mixed(a: i64, b: f64) -> Result<f64, RuntimeError> {
    let total: f64 = PyAdd::py_add(&(((a) as f64)), &(b))?;
    Ok(total)
}

pub fn scale(a: f64, b: f64) -> Result<f64, RuntimeError> {
    Ok(PyNum::py_mul(&(a), &(b))?)
}

pub fn widen(n: i64) -> Result<f64, RuntimeError> {
    Ok(((n) as f64))
}
